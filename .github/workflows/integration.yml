name: contract-ci

on:
  pull_request:
  push:
    tags:
    branches:
      - 'main'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          repository: 'envoylabs/whoami'
          ref: 'v0.3.2'
      - name: Run deploy script
        run: |
          chmod a+x ./scripts/deploy_local.sh
          ./scripts/deploy_local.sh juno16g2rahf5846rxzp3fwlswy08fz8ccuwk03k57y

